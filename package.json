{
  "name": "asset-system-monolith",
  "version": "1.0.0",
"scripts": {
  "install-all": "npm install --prefix backend && cd frontend && npm install",
  "render-deploy": "npm install --prefix backend && npx knex migrate:latest --knexfile backend/knexfile.js",
  "start": "node backend/server.js"
}
}